# Configuración FTP para GoDaddy
# Copia este archivo a .env y completa con tus credenciales

# Host FTP de GoDaddy (generalmente es tu dominio con ftp. al inicio)
FTP_HOST=ftp.tudominio.com

# Puerto FTP estándar
FTP_PORT=21

# Usuario FTP (generalmente es tu usuario de cPanel o el email asociado)
FTP_USER=tu-usuario@tudominio.com

# Contraseña FTP (¡NO compartas este archivo!)
FTP_PASSWORD=tu-contraseña-aquí

# Ruta remota donde se publicará el sitio
# Para GoDaddy, el directorio público generalmente es /public_html
# Si tienes un subdirectorio, sería /public_html/subdirectorio
FTP_REMOTE_PATH=/public_html

# Protocolo (para GoDaddy usa 'ftp')
FTP_PROTOCOL=ftp
